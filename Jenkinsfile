pipeline {
    agent any

    stages {

        stage('Check Files') {
            steps {
                sh 'pwd'
                sh 'ls -la'
            }
        }
        stage('Check Files') {
            steps {
                sh 'pwd'
                sh 'ls -la'
            }
        }

    }
}
